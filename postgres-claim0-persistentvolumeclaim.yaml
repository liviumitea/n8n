kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: n8n-postgresql-pvc
  namespace: n8n
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  volumeName: "n8n-postgresql-pv"
